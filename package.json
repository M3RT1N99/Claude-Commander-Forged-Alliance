{
  "name": "claude-commander-fa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Commander: Forged Alliance — browser reimplementation of Supreme Commander: Forged Alliance. Bring your own game files.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "typecheck:scripts": "tsc --noEmit -p tsconfig.scripts.json",
    "test": "tsx scripts/run-tests.ts"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "three": "^0.166.0",
    "wasmoon": "^1.16.0"
  },
  "devDependencies": {
    "@types/luaparse": "^0.2.13",
    "@types/node": "^26.4.0",
    "@types/three": "^0.166.0",
    "luaparse": "^0.3.1",
    "tsx": "^4.23.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0"
  }
}
